use std::Path;
use std::File;
use std::env;


struct Scoop {
    script: Path,
}

impl Scoop {
    pub fn download(&self) {

    }
}

pub fn install() {
    let scoop = Scoop {
        script: Path::new("),
    }
}
